# API DTO

This workspace is reserved for the API contract between the frontend and backend.

The contract format is intentionally not selected yet. OpenAPI, TypeScript DTOs, validation schemas, and code generation can be added after the project chooses that direction.
